package com.gcancino.levelingup.domain.models.identity

enum class StandardType {
    // Physical & Vitality
    TRAINING,
    NUTRITION,
    SLEEP,
    // Mental & Discipline
    MINDSET,
    LEARNING,
    DEEP_WORK,
    // Character & Social
    FINANCE,
    // Catch-all
    CUSTOM
}